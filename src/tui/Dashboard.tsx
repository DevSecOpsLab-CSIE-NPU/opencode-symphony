#!/usr/bin/env bun

/**
 * Interactive Terminal UI Dashboard (Ink-based)
 * Vim/OpenCode style keyboard navigation
 */

import { Text } from 'ink';
export default function Dashboard() {
  return <Text bold>Symphony Issue Dashboard</Text>;
}
